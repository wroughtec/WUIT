import template from './marketing.html';
import controller from './marketing.controller';

let marketingComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default marketingComponent;
